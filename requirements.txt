# JARVIS - Dependencias principales (modo texto, sin voz)
# Python 3.11+

# LLM
openai>=1.40.0
anthropic>=0.34.0
requests>=2.31.0

# Automatización y control de PC
pyautogui>=0.9.54
pygetwindow>=0.0.9
pyperclip>=1.9.0
psutil>=5.9.8
pillow>=10.4.0

# Audio del sistema (Windows)
pycaw>=20240210; platform_system=="Windows"
comtypes>=1.4.6; platform_system=="Windows"

# Brillo de pantalla (Windows)
screen-brightness-control>=0.24.0; platform_system=="Windows"

# UI
rich>=13.7.1

# Web chat (uvicorn[standard] incluye websockets + wsproto para WS)
fastapi>=0.112.0
uvicorn[standard]>=0.30.0
websockets>=12.0,<16.0
wsproto>=1.2.0
httpx>=0.27.0

# Configuración
python-dotenv>=1.0.1

# Notificaciones
plyer>=2.1.0

# Tests
pytest>=8.0.0

# GUI Opcional: Widget de escritorio con esfera reactiva
# Instalar solo si se usa --gui: pip install PyQt6
PyQt6>=6.6.0; extra == "gui"
